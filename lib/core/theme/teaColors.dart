import 'package:flutter/material.dart';

class TEAColors {
  // Fondos y superficies
  static const background = Color(0xFFFAFAFA);
  static const cardWhite = Colors.white;
  static const inputBackground = Color(0xFFF5F5F5);
  static const border = Color(0xFFE0E0E0);

  // Base de la app
  static const seedGreen = Color(0xFFC8E6C9);

  // Colores de texto
  static const textPrimary = Color(0xFF424242);
  static const textSecondary = Color(0xFF616161);

  // Colores de acento (Estilo Pasteles)
  static const chickyYellow = Color(0xFFFFF9C4); // El círculo del pollito
  static const bluePastel = Color(0xFF90CAF9);   // Botones de acción
  static const greenPastel = Color(0xFFA5D6A7);  // Confirmaciones
  static const successPastel = Color(0xFFA5D6A7);
  static const errorPastel = Color(0xFFEF9A9A);
  static const pinkPastel = Color(0xFFF8BBD0);
  static const lightBluePastel = Color(0xFFBBDEFB);
  static const peachPastel = Color(0xFFFFE0B2);
  static const lavenderPastel = Color(0xFFE1BEE7);
  static const orangeAccent = Colors.orangeAccent;
}